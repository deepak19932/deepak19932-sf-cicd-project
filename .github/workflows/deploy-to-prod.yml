name: Deploy to Production

on:
  pull_request:
    branches: [main]
    types: [closed]

jobs:
  deploy:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Salesforce CLI
        run: npm install -g @salesforce/cli

      - name: Authenticate to Prod Org
        run: |
          echo "${{ secrets.PROD_SFDX_AUTH_URL }}" > sfdx_auth_url.txt
          sf org login sfdx-url --sfdx-url-file sfdx_auth_url.txt --alias prod-org --set-default
          rm -f sfdx_auth_url.txt

      - name: Verify authentication
        run: sf org display --target-org prod-org

      - name: Deploy to Production
        run: |
          sf project deploy start \
            --source-dir force-app \
            --target-org prod-org \
            --wait 30

      - name: Run Apex Tests
        run: |
          TEST_CLASSES=$(find force-app -name "*Test*.cls" 2>/dev/null | sed 's|.*/||;s|\.cls||' | tr '\n' ',' | sed 's/,$//')
          if [ -n "$TEST_CLASSES" ]; then
            echo "Running tests: $TEST_CLASSES"
            sf apex run test \
              --target-org prod-org \
              --tests $TEST_CLASSES \
              --wait 30 \
              --result-format human
          else
            echo "No Apex test classes found, skipping tests."
          fi
